# aideom-vn-app
# Tạo thư mục dự án
mkdir aideom-vn-app && cd aideom-vn-app

# Tạo virtual environment
python -m venv venv
venv\Scripts\activate     # Windows
# source venv/bin/activate # Mac/Linux

# Tạo requirements.txt
